package com.vti.Assignment5.EX1;

public class QuickSort /*extends SequenceNumber implements Sort*/ {

	/*
	 * int[] seSort;
	 * 
	 * QuickSort(int n) { super(n); super.sequences = seSort; }
	 * 
	 * @Override public void sort() { quick(sequences, 0, sequences.length - 1);
	 * 
	 * }
	 * 
	 * public void quick(int[] se, int head, int end) { int pivot = sequences[(head
	 * + end) / 2]; int i = head, j = end;
	 * 
	 * while (i <= j) { while (se[i] < pivot) { i++; } while (se[j] > pivot) { j--;
	 * } if (i <= j) { int temp = se[i]; se[i] = se[j]; se[j] = temp; i++; j--; } }
	 * if (head < j) quick(se, head, j);
	 * 
	 * if (end > i) quick(se, i, end);
	 * 
	 * }
	 */
}
